@model int?
@(Html.Kendo().ComboBoxFor(m => m)

                         .DataTextField("Name")
                         .DataValueField("Id")
                         .Placeholder("Select Journal")
                         .Filter(FilterType.Contains)
                          .Template("<span class='k-state-default'  style='margin-right:10px'>#: data.Code #</span><span class='k-state-default'>#: data.Name #</span>")

                         .DataSource(source =>
                         {
                             source.Read(read =>
                             {
                                 read.Action("FillJournals", "GenerlaClasses");
                             });
                         })

                              .Events(e =>
                             {
                                 e.Select("JournalGridSelect");
                                 e.DataBound("JournalGridDataBound");
                                 e.Change("JournalGridChange");


                                 //  .Open("open")
                                 //   .Close("close")
                             })


)
